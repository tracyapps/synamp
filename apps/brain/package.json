{
  "name": "@synamp/brain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SynAmp brain — playlists, library intelligence, and playback session state.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node --experimental-strip-types src/index.ts",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.2"
  }
}
